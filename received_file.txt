시나리오

1. 폴더에 파일을 읽는다
2. 읽은 파일 내용중 특정 문구가 있으면 1번 메소드를 실행하고, 또 다른 문구가 있으면 2번 메소드 또다른 문구는 3번 이런식으로 진행한다.
3. 결과를 MAP에 담고, 해당 MAP에 SET으로 값을 추가한다.
4. FOR문으로 하나씩 꺼내면서 값을 바꾼다.
5. 바꾼 값을 DB에 저장한다. (옵션)
6. 예외 처리는 특정 메소드를 실행하도록 구성한다.